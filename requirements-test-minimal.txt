# Minimal requirements for Python 3.13 - all versions tested and compatible
# Tests use SQLite in-memory databases (no PostgreSQL required)

# Testing Framework
pytest>=8.3.0
pytest-cov>=6.0.0
pytest-mock>=3.14.0
pytest-asyncio>=0.24.0
httpx>=0.28.0

# Core Framework
fastapi>=0.115.0
slowapi>=0.1.9
starlette>=0.41.0

# Database
sqlalchemy>=2.0.36

# Configuration & Validation
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Security (password hashing)
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0
