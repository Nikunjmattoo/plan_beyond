# Minimal requirements for running foundation tests
# These should all have Python 3.13 wheels available

# Testing (Python 3.13 compatible)
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-asyncio>=0.23.0
httpx>=0.25.2

# Core Framework (needed by conftest)
fastapi>=0.110.0
slowapi>=0.1.9

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Configuration & Validation (Python 3.13 compatible)
pydantic>=2.8.0
pydantic-settings>=2.3.0

# Security (for password hashing in fixtures)
passlib[bcrypt]==1.7.4
bcrypt==4.1.1
